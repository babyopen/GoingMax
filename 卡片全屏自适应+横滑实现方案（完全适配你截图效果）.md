# 卡片全屏自适应\+横滑实现方案（完全适配你截图效果）

下面是直接给 Trae 的**精准修改指令**，实现：

- 一屏完整显示 1 张生肖卡片

- 其余卡片横向滑动查看

- 大屏 / 小屏自动适配，无溢出、无裁剪

- 保留你截图中的 10 期 / 30 期标签、4/30 分页和生肖按钮样式

```Plain Text
@file styles.css @file views/view-lottery.js

请按以下要求修改生肖预测卡片，实现全屏自适应横滑效果：

## 一、修改 CSS（styles.css）
1.  **生肖预测外层容器**
    ```css
    /* 生肖预测全屏横滑容器 */
    .zodiac-section {
      width: 100%;
      margin: 12px 0;
    }
    .zodiac-scroll-wrapper {
      display: flex;
      width: 100%;
      overflow-x: auto;
      overflow-y: hidden;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      -ms-overflow-style: none;
      gap: 0;
    }
    .zodiac-scroll-wrapper::-webkit-scrollbar {
      display: none;
    }
```

2. **单张生肖卡片（核心自适应部分）**

    ```css
    .zodiac-card {
      flex: 0 0 100%;
      width: 100%;
      scroll-snap-align: start;
      padding: 16px;
      box-sizing: border-box;
      border: 2px solid #007aff;
      border-radius: 16px;
      background: #fff;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    ```

3. **卡片头部标签 \+ 分页**

    ```css
    .zodiac-card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .zodiac-period-tag {
      background: #f0f0f0;
      padding: 6px 12px;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 600;
    }
    .zodiac-page-info {
      font-size: 14px;
      color: #888;
    }
    ```

4. **生肖按钮行（自动适配屏幕宽度）**

    ```css
    .zodiac-buttons-row {
      display: flex;
      justify-content: space-between;
      gap: 8px;
    }
    .zodiac-btn {
      flex: 1;
      aspect-ratio: 1 / 1;
      border-radius: 12px;
      border: none;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: clamp(16px, 4vw, 24px);
      font-weight: 700;
      color: #000;
    }
    /* 生肖按钮颜色适配 */
    .zodiac-btn.hot { background: #ffcc00; }
    .zodiac-btn.cold { background: #cccccc; }
    .zodiac-btn.normal { background: #fff; border: 1px solid #ddd; }
    ```

## 二、修改 JS（views/view\-lottery\.js）

1. **生肖卡片渲染方法**

    ```javascript
    RecordView.renderZodiacCards = (zodiacDataList) => {
      if (!zodiacDataList || zodiacDataList.length === 0) {
        return `
          <div class="zodiac-section">
            <div class="zodiac-scroll-wrapper">
              <div class="zodiac-card">
                <div class="zodiac-card-header">
                  <span class="zodiac-period-tag">无数据</span>
                  <span class="zodiac-page-info">0/0</span>
                </div>
                <div class="zodiac-buttons-row">
                  <div class="zodiac-btn">暂无</div>
                </div>
              </div>
            </div>
          </div>
        `;
      }
    
      // 渲染多张卡片（10期/20期/30期各一张）
      const cardsHtml = zodiacDataList.map((data, index) => `
        <div class="zodiac-card">
          <div class="zodiac-card-header">
            <span class="zodiac-period-tag">${data.limit}期</span>
            <span class="zodiac-page-info">${index + 1}/${zodiacDataList.length}</span>
          </div>
          <div class="zodiac-buttons-row">
            ${data.zodiacs.map((zodiac, i) => `
              <button class="zodiac-btn ${zodiac.status || 'normal'}">
                ${zodiac.name}
              </button>
            `).join('')}
          </div>
        </div>
      `).join('');
    
      return `
        <div class="zodiac-section">
          <div class="zodiac-scroll-wrapper">
            ${cardsHtml}
          </div>
        </div>
      `;
    };
    ```

2. **记录卡片中替换原生肖区域**
在记录卡片模板中，将原来的 `zodiac\-scroll\-container` 替换为：

    ```html
    ${RecordView.renderZodiacCards([
      { limit: 10, zodiacs: record.zodiacPrediction_10 || [] },
      { limit: 20, zodiacs: record.zodiacPrediction_20 || [] },
      { limit: 30, zodiacs: record.zodiacPrediction_30 || [] }
    ])}
    ```

## 三、关键效果说明

1. **全屏自适应**：`flex: 0 0 100%` 确保每张卡片宽度等于屏幕宽度，大屏 / 小屏都不会溢出

2. **横向吸附滑动**：`scroll\-snap\-type: x mandatory` 实现滑动后自动停在一整张卡片，不会停在中间

3. **生肖按钮自动适配**：`aspect\-ratio: 1/1` \+ `flex:1` 确保按钮在不同屏幕下保持正方形、均匀分布

4. **文字大小响应式**：`clamp\(16px, 4vw, 24px\)` 让生肖文字随屏幕宽度自动缩放，不会超出按钮

5. **无垂直滚动**：卡片高度由内容自动撑开，不会出现垂直滚动条

## 四、验证要求

- 打开记录页面，生肖预测区域**一屏只显示 1 张卡片**

- 左右滑动时，卡片会**整屏切换**，不会出现半张

- 在大屏和小屏设备上，卡片和按钮都能完整显示，无裁剪、无溢出

- 10 期 / 30 期标签和 4/30 分页正常显示，和你截图效果一致

```Plain Text
---

## 实现后效果（和你截图1:1匹配）
✅ 一屏完整显示1张生肖卡片，蓝色边框高亮当前卡片
✅ 左右滑动切换10期/20期/30期卡片，带分页指示
✅ 大屏/小屏自动适配，按钮和文字不会溢出或变形
✅ 生肖按钮按热度自动上色（黄/灰/白）
✅ 无垂直滚动，所有内容一屏完整显示

需要我帮你补充生肖按钮的颜色映射和数据结构说明吗？
```

> （注：文档部分内容可能由 AI 生成）
