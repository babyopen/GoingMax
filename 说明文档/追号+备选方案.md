主方案 + 备选方案 完整两套筛选+排序规则（可直接落地接入代码）

前置统一约定

• 主方案固定输出：4个

• 备选方案固定输出：2个

• 合并去重后最多：6个

• 备选方案只展示、不参与追号、不允许新开追号组

#### 一、两套共用同一套历史数据、行情窗口，只改参数 + 排序优先级&#xA;一、共用前置步骤（主/备选都走一样）

1. 获取当前行情12期观测窗口
2. 按配置动态剔除近期期数
3. 剩余生肖做频次、遗漏统计

#### 再按各自规则筛选、排序、截取个数

#### 二、主方案规则（保留你原版逻辑不动）

1. 行情参数（沿用现有）

• 统计周期：热28 / 常态27 / 震荡29 / 冷32

• 高频阈值：hot/normal≥3 ｜ shock/cold≥2

• 动态剔除期数：按原有4态配置

2.排序优先级（从高到低）

高频频次降序 → 远距离隔期回补 → 中等遗漏 → 浅遗漏杂号

3.输出规则

筛选后按序截取前 4个 作为主推荐

三、备选方案专属规则（全新保守版）

1.备选固定参数（不分行情，统一保守）

• 固定统计回看：34期

• 高频阈值：统一 ≥2

• 动态剔除：在当前行情原剔除期数 +2期

◦ 原剔3 → 备选剔5

◦ 原剔5 → 备选剔7

◦ 原剔7 → 备选剔9

2.备选排序优先级（和主方案反向，避热门、重深遗漏）

超长深度遗漏（优先） → 低频次稳健号 → 弱化短期高频
核心逻辑：不追当下热码，只蹲超跌休眠修复，专治震荡尾、冷市中后期、风控连错期

3\. 输出规则

按备选规则筛选排序后，跳过主方案已有的号
再顺次截取 2个全新不重复 作为备选推荐

#### 四、关键去重逻辑（内置在备选生成里）

1. 先生成主方案4个
2. 备选候选池排序完成后
3. 过滤掉 已在主方案里的生肖
4. 从剩余里面拿前2个

   👉 保证：主4、备选2 完全不重复，合并刚好6个，无需再二次去重
5. 五、可直接写进项目的配置结构
   /

   // strategy.config.js

   module.exports = {

   // 推荐数量配置

   recommendCount: {
   main: 4,
   backup: 2,
   maxCombine: 6
   },

// 主方案参数（沿用现有行情参数即可，无需重写）
main: {},

// 备选方案固定保守参数
backup: {
lookBackPeriod: 34,
minFreq: 2,
filterOffset: 2 // 剔除期数在原基础+2
}
};

六、核心判断复用函数（直接可用）

/\*\*

\*是否需要启用备选方案

\*/

function needBackupStrategy(isRiskPause, market, cycleStage) {
return isRiskPause
|| (market === 'shock' && cycleStage === 'late')
|| (market === 'cold' && \['mid', 'late'].includes(cycleStage));
}

七、业务层整体流程定稿

1. 计算行情、周期阶段、风控状态
2. 判断是否需要备选
3. 按主方案规则生成固定4个
4. 需要备选时：

◦ 按备选固定参数+排序规则生成候选

◦ 过滤掉主方案已存在的

◦ 取前2个做备选

5.展示：主4 + 备选2 = 共6个

6.追号逻辑：

◦ 只认主4个

◦ 有备选期间禁止新开追号组

八、我可以下一步直接给你

封装好的两个函数：

• genMainRecommend() 生成主4个

• genBackupRecommend(mainList) 自动避重、生成备选2个
